[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"


[project]
name = "recomart"
version = "0.1.0"
description = "Production-oriented recommender system"
requires-python = ">=3.11"


[tool.setuptools.packages.find]
where = ["."]